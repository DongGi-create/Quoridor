<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/dirtfy_item_bg"
    android:padding="10dp"
    android:layout_marginHorizontal="10dp"
    android:layout_marginVertical="5dp">

    <ImageView
        android:id="@+id/opponent_profile_image_view"
        android:layout_width="0dp"
        android:layout_height="50dp"
        android:src="@drawable/hobanwoo_blue"
        app:layout_constraintDimensionRatio="1:1"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"/>

    <TextView
        android:id="@+id/opponent_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/D_text"
        android:layout_marginStart="10dp"
        android:textSize="20sp"
        android:lines="1"
        android:maxWidth="150sp"
        android:ellipsize="end"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toEndOf="@id/opponent_profile_image_view"
        tools:text="didwoah"/>

    <TextView
        android:id="@+id/opponent_rating"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/D_text"
        android:layout_marginStart="10dp"
        app:layout_constraintBottom_toBottomOf="@id/opponent_name"
        app:layout_constraintStart_toEndOf="@id/opponent_name"
        tools:text="1500"/>

    <ImageView
        android:id="@+id/result_iamge_view"
        android:layout_width="50dp"
        android:layout_height="50dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        tools:src="@drawable/baseline_lens_24_green"/>

</androidx.constraintlayout.widget.ConstraintLayout>